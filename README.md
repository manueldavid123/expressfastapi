# EXPRESS TEMPERATURE API

**USE:**

    * npm install
    * npm start

**EXAMPLE**

`    curl -X POST \
  -k http://localhost:4000/temperature \
  -H 'Content-Type: application/json' \
  -H 'cache-control: no-cache' \
  -d '{"temperature":"22"}'`
